interface JokerState {
  half: {
    used: boolean;
  };
  audience: {
    used: boolean;
  };
  switchQuestion: {
    used: boolean;
  };
  search: {
    used: boolean;
  };
}

export default JokerState;
